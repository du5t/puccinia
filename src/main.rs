#![feature(ip)] // unstable is_loopback feature in ip
#![feature(ip_addr)] // unstable ip addr types
extern crate bufstream;
extern crate chrono;

mod controlsocket;

fn main() {
    // let address = "127.0.0.1:9050".parse().unwrap();
    // let server = TcpSocket::bind(&address).unwrap();

}
